"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";
export default function Login(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const d=new FormData(e.currentTarget);try{await api("/auth/login",{method:"POST",body:JSON.stringify({email:d.get("email"),password:d.get("password")})});router.push("/play");router.refresh()}catch(x){setError((x as Error).message)}finally{setBusy(false)}}return <div className="authShell"><form className="authCard stack" onSubmit={submit}><div><p className="eyebrow">WELCOME BACK</p><h1>继续你的故事</h1><p style={{color:"var(--muted)"}}>登录后，你的世界与存档只属于这个账号。</p></div><div className="field"><label htmlFor="email">邮箱</label><input className="input" id="email" name="email" type="email" autoComplete="email" required/></div><div className="field"><label htmlFor="password">密码</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" required/></div>{error&&<p className="error" role="alert">{error}</p>}<button className="button primary" disabled={busy}>{busy?"正在登录…":"登录"}</button><p style={{fontSize:13,color:"var(--muted)"}}><Link className="textLink" href="/forgot-password">忘记密码？</Link></p><p style={{fontSize:13,color:"var(--muted)"}}>还没有账号？ <Link className="textLink" href="/register">免费注册</Link></p></form></div>}
