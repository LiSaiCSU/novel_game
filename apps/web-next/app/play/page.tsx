"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {api} from "@/lib/api";
type Play={id:string;name:string;status:string;preview:boolean;updated_at:string;release:{id:string;title:string}};
export default function PlayLibrary(){const[items,setItems]=useState<Play[]>([]);const[error,setError]=useState("");useEffect(()=>{api<Play[]>("/playthroughs").then(setItems).catch(e=>setError(e.message))},[]);return <div className="page"><div className="pageHead"><div><p className="eyebrow">MY STORIES</p><h1>我的故事</h1><p>每一局都固定在开始时的作品版本上，你可以放心继续。</p></div><Link className="button primary" href="/library">寻找新作品</Link></div>{error?<div className="panel"><p className="error">{error}</p><Link className="textLink" href="/login">登录后查看 →</Link></div>:items.length===0?<div className="empty"><h2>书架还是空的</h2><p>从公开作品库选择一个世界开始。</p></div>:<div className="cardGrid">{items.map(x=><Link href={`/play/${x.id}`} className="workCard" key={x.id}><div className="workBody"><div className="meta"><span>{x.preview?"预览":"正式存档"}</span><span>{x.status}</span></div><h2>{x.release.title}</h2><p>{x.name}</p><small>最后更新 {new Date(x.updated_at).toLocaleString()}</small></div></Link>)}</div>}</div>}
